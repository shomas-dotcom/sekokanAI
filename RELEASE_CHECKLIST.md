# リリース前チェックリスト

2026-09-26 時点。販売開始(他社に使ってもらう)までに必要なことを、
「済み」「杉本さんの手続きが必要」「決めてほしいこと」に分けて書く。

## 済み(2026-09-26 点検)

- [x] 型チェック・書き方チェック(eslint)・本番ビルドが通る
- [x] 自動試験 222件がすべて通る(試験用DBで、DBに書き込む試験も含めて確認)
- [x] Next.js の重大な脆弱性を修正(16.3.0 → 16.3.6)
- [x] 本番で「デモ用プラン切替」「疑似トライアル」を使えないようにした
      (これまでは誰でも無料で有料プランにできた)
- [x] 自動試験が本番DBに書き込まないようにした
- [x] 本番DBに試験用の会社が残っていないことを確認(会社数3、試験用0)
- [x] `.env`・DBファイルがGitに入っていないこと、履歴に秘密の鍵が無いことを確認

## 杉本さんの手続きが必要(コードでは代われない)

| # | やること | なぜ | どこで |
|---|---|---|---|
| 1 | ~~今回の修正を本番に反映~~ | 2026-09-26 反映済み(`main` に16件を反映) | 済み |
| 1b | ~~Renderを有料(Starter、月$7)に切り替える~~ | 2026-09-26 切替済み。21分放置後も0.5秒で表示されることを確認 | 済み |
| 2 | Stripe(カード決済)の本申し込み、月額商品の作成、キー3つ(`STRIPE_SECRET_KEY` / `STRIPE_PRICE_ID` / `STRIPE_WEBHOOK_SECRET`)をRenderに設定、Webhookの送り先を本番URLに設定 | これが無いとお金を受け取れない。今は「受付準備中」と表示される | stripe.com と Render の環境変数画面 |
| 3 | 特定商取引法の表記: 事業者名・運営責任者・所在地・価格(税込)・メールは2026-09-26反映済み。**残り: 電話番号** | 特商法では電話番号の表示も必要 | 番号を送ってもらえばClaudeが反映(`src/app/tokushoho/page.tsx`) |
| 4 | お問い合わせ先: メールは2026-09-26反映済み。**残り: 電話番号(載せるなら)** | | `src/app/contact/page.tsx` |
| 4b | 運営画面でスタンダード/プロを「新規契約の選択肢に表示しない」にする | 料金はライトのみに確定したため。本番DBの変更なので本人が画面で操作 | 本番の /admin → 料金プラン |
| 5 | 利用規約・プライバシーポリシーを専門家に見てもらう | いずれも仮文面 | `src/app/terms`・`src/app/privacy` |
| 6 | `デスクトップ\杉本土木\社内\APIキー.xlsx` の扱いを決める | APIキーが平文で入っている。OneDriveなのでクラウドにも保存されている。漏れると勝手に使われ請求が来る | 不要なら削除。必要ならパスワード管理の仕組みへ |
| 7 | メール送信(Resend)のキーと送信元アドレスの設定 | 未設定だと、登録確認・パスワード再設定のメールが届かない | Render の `RESEND_API_KEY`・`EMAIL_FROM` |

## 決めてほしいこと

| # | 内容 | 判断材料 |
|---|---|---|
| A | ~~料金~~ → 月額9,800円(税込)・ライトのみに確定(2026-09-26)、画面に反映済み | Stripeの商品も9,800円(税込)で作ること |
| B | ~~Renderの無料プラン~~ → 有料(Starter)に決定(2026-09-26) | 切替は上の1b |
| C | 写真をどこに保存するか | 今は写真をDBにそのまま保存(1枚最大8MB、縮小なし)。Neon無料枠は0.5GBなので、スマホ写真(1枚3〜4MB)だと約100〜150枚で一杯になる。案: ①写真を長辺2000pxに縮小して保存(県の写真管理基準は100万〜300万画素程度でよい。ただし「編集は認めない」との関係を確認要) ②外部の保存場所(有料になる場合あり)に移す |
| D | 独自ドメインを取るか | 今は `genba-ai-app.onrender.com` |

## 残した小さな課題

- Prisma CLI経由の mysql2 / deepmerge-ts、exceljs経由の uuid の脆弱性(CHANGELOG参照)
- `.env` の接続文字列の `sslmode=require` について将来の仕様変更の警告が出る。
  今の動きを続けるなら `sslmode=verify-full` に書き換える(本番のRender側も同様)
