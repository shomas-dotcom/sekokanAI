# バックアップ・復旧

## 開発環境(現状)

SQLiteファイル(`prisma/dev.db`)はローカルのみに存在し、gitignore対象(意図的 — 開発データは使い捨て前提)。デモデータは `npm run db:seed` でいつでも再現できる。

## 本番環境(Day6-7でクラウドDB確定後に整備)

- クラウドDB(Neon/Supabase/Turso等)の自動バックアップ機能を有効化する(多くのサービスが日次スナップショットを標準提供)
- マイグレーション履歴(`prisma/migrations/`)はGit管理下にあるため、スキーマ自体はリポジトリから常に復元可能
- 顧客の見積/日報/施工計画書データは事業継続上重要なため、バックアップ保持期間を最低30日とする(利用サービスのプラン次第で調整)

## 復旧手順(想定)

1. 障害発生時、クラウドDBサービスの管理画面から直近の正常なスナップショットを確認する
2. 新しいDBインスタンスへスナップショットを復元する
3. `DATABASE_URL` を復元先に切り替え、デプロイ先の環境変数を更新して再デプロイする
4. [TEST_PLAN.md](./TEST_PLAN.md) のスモークテストで復旧を確認する
5. 復旧内容と原因を [CHANGELOG.md](./CHANGELOG.md) に記録する

## 未確定事項

具体的なバックアップ頻度・保持期間・RTO/RPOはクラウドDBサービス確定後(Day6-7)に本ドキュメントを更新する。
