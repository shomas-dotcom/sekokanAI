# テスト計画

## 実施するテスト種別

- 単体テスト(価格区分ロジック、日報の不足項目検出、日付/数値パース等)
- 統合テスト(Server Actions ↔ Prisma ↔ DB)
- E2Eテスト(ログイン→見積作成→PDF出力の一連の流れ)
- ログインテスト(正しい/誤ったパスワード、未登録メール)
- 権限テスト(MEMBERが会社設定を変更できないこと)
- 会社間データ分離テスト(A社のユーザーがB社の顧客/案件/見積にアクセスできないこと — 最優先で自動化する)
- PDF出力テスト(見積PDFの項目・合計金額の整合性)
- スマートフォン表示テスト(主要画面をモバイル幅で確認)
- 音声入力テスト(認識結果の各項目への割当、認識不能時に確認候補として表示されること)
- AI出力テスト(下記「AI出力の確認事項」)
- 入力漏れテスト(必須項目未入力時のエラー表示)
- 異常系テスト(不正なファイル形式アップロード、極端に大きい数量値等)
- ファイルアップロードテスト(拡張子/サイズ制限)
- セキュリティテスト(SQLインジェクション・XSS入力のサニタイズ確認、認証無しでのServer Action直接POST拒否)

## AI出力の確認事項

- 事実と推測を区別しているか(`assumedItemsJson` 等で明示されているか)
- 存在しない情報を作っていないか(架空の数量・単価・法令名等)
- 危険な施工方法を出していないか
- 法令違反につながる内容がないか
- 専門用語が不自然でないか(建設業の用語として妥当か)
- 提出書類として不足がないか(施工計画書の必須章立てが揃っているか)

## 現状(Day6)

Vitestを導入した(`npm run test`)。現時点では以下を自動テスト化している:

- 単体テスト: 消費税の端数処理(切り捨て/四捨五入/切り上げ)、請求金額の累計・残額計算、契約書の18条項生成(`src/lib/__tests__/calc.test.ts`, `contractClauses.test.ts`)
- 結合テスト: 自動採番の重複防止(同時に10件採番しても番号が重複しないこと)、会社ごとの独立性(`src/lib/__tests__/numbering.test.ts`。専用の一時SQLiteファイルを使い、dev.dbを汚さない)

Playwrightによる自動E2Eテストは未導入。今回はブラウザ操作(ログイン→ダッシュボード→契約書詳細/印刷/DOCX出力→請求書詳細/印刷→会社設定→案件詳細の書類一覧)を手動で確認した。Day7以降で自動E2E化を検討する。

## 実行済みの確認(Day6)

- `npx tsc --noEmit` — 型エラー無し
- `npm run lint` — 新規コードにエラー無し(既存コードに無関係な既知の警告/エラーが2件あり、別途対応予定)
- `npm run test` — 3ファイル14件すべて成功
- `npm run build` — 本番ビルド成功
- `npm run db:seed` — 見積→契約→請求まで連携したデモデータの投入を確認(再実行しても採番が重複しないことを確認)
- ブラウザでの一連操作確認(本番ビルドを別ポートで起動して確認。理由: 開発サーバーは他セッションが起動中で、Prismaスキーマ変更前の古いクライアントをメモリに保持していたため)

## 実行済みの確認(Day1)

- `npx tsc --noEmit` — 型エラー無し
- `npm run build` — 本番ビルド成功
- `npm run db:seed` — Prismaスキーマの実DB(SQLite)への適用・書き込み・読み込みを確認
