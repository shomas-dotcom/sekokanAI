# セキュリティ方針

## 実施事項

- 認証(Day2でAuth.js導入)・認可(ロールベース: ADMIN/MEMBER)
- 会社ごとのデータ分離(全業務テーブルに `companyId`、[ARCHITECTURE.md](./ARCHITECTURE.md))
- パスワードのハッシュ化(Node組み込み `crypto.scrypt`、[src/lib/password.ts](./src/lib/password.ts)。ソルト付き、平文・可逆暗号化は行わない)
- 通信の暗号化(本番はHTTPS必須、デプロイ先のTLS終端に依存)
- 入力値検証(サーバー側のServer Actionsで必ず再検証。クライアント側検証のみに依存しない)
- SQLインジェクション対策(Prismaのパラメータ化クエリのみを使用し、生SQLの文字列結合を行わない)
- XSS対策(Reactのデフォルトエスケープに依存し、`dangerouslySetInnerHTML`を使用しない)
- CSRF対策(Server Actionsは同一オリジンPOSTのみ受理するNext.jsの既定動作に依存。加えて各Server Action内で毎回セッション検証を行う — Server Functionsは直接POSTで到達可能なため)
- ファイルアップロード制限(拡張子/MIME/サイズ上限を実装時に定義、優先度B「写真アップロード」で対応)
- アクセスログ・操作ログ・エラーログ(`AuditLog` モデル。個人情報・口座情報・顧客情報の本文は記録しない)
- レート制限(認証エンドポイントを優先して実装、Day2〜)
- 管理者権限の分離(`UserRole.ADMIN` のみ会社設定変更・ユーザー招待が可能)
- 秘密情報の環境変数管理([ENVIRONMENT_VARIABLES.md](./ENVIRONMENT_VARIABLES.md)、`.env*` は `.gitignore` 対象)
- 個人情報の最小保存(顧客情報は氏名・連絡先等の業務上必要な項目に限定)
- バックアップ・復旧手順([BACKUP_AND_RECOVERY.md](./BACKUP_AND_RECOVERY.md))
- 退会時のデータ削除手順(実装時に定義、優先度B以降)
- 利用規約・プライバシーポリシーへの同意(登録フローに組み込む、Day2〜)

## 禁止事項

- APIキー・口座情報・パスワードの直書き(コード/コミット/ログへの記載禁止)
- 本番環境への無断デプロイ
- 既存ホームページの無断変更
- 有料サービスへの無断登録
- 外部への無断送信
- 顧客情報のテスト利用(テストデータは架空の値のみ使用する。[prisma/seed.ts](./prisma/seed.ts) はダミー値)
- 実績の捏造、架空レビューの作成

## 決済・口座情報の扱い

銀行口座情報(銀行名・支店名・口座種別・口座番号・口座名義・法人番号)、クレジットカード情報、APIキー、パスワード、秘密鍵は、プロンプト・ソースコード・GitHub・DB初期値・ログ・テストデータに記載しない。開発段階ではダミーデータのみ使用する。

本番公開時は、サービス管理画面または決済サービス(Stripe等)の管理画面から事業者本人が登録する構成とする。振込先情報を画面表示する場合はマスキングする(例: 普通 ****1966)。

決済機能実装時はテストモード/本番モードを分離し、決済成功・失敗・返金・キャンセル・請求書発行・領収書発行・入金予定/済み/未入金・売上/月次集計に対応する。

## 外部サービスへの操作の確認フロー

以下の操作は、最終確定前に必ず確認画面(実行内容・対象・金額・送信先・公開先・取消可否・想定される影響を表示)を経る:

外部へのメール送信、Instagram投稿、ホームページ投稿、請求書送信、契約書送信、決済実行、返金、本番公開、顧客データ削除、有料サービス契約、本番APIキーの登録。
